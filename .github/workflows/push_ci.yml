name: test ci

on:
  push

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Checkout Branch
        uses: actions/setup-node@v2

      - name: Test step 1
        run: |
          mkdir xbqtest233

